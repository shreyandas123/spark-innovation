export const metadata = {
  title: "My Wishlist",
  description: "Your saved Kutchina kitchen appliances. Add items to cart or continue browsing.",
  robots: { index: false, follow: true },
};

export default function WishlistLayout({ children }) {
  return children;
}
